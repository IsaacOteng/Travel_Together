// Assets must be imported so Vite fingerprints them into dist/assets on build.
// Referencing "/src/assets/..." directly only works under the dev server and
// 404s in production.
import officialLogo from "./official_logo_nobg.png";
import ttLogo from "./ttlogo.png";

export { officialLogo, ttLogo };
